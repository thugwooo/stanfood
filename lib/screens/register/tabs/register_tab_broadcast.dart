import 'package:flutter/widgets.dart';

class RegisterTabBroadcast extends StatelessWidget {
  const RegisterTabBroadcast({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
