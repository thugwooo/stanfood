import 'package:flutter/widgets.dart';

class RegisterTabMain extends StatelessWidget {
  const RegisterTabMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
